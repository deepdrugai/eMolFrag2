[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "eMolFrag2"
version = "2.0.0.2024.05.19"
dependencies = ["rdkit", "colorlog", "networkx", "pandas", "matplotlib"]
requires-python = ">=3.7"
authors = [
    { name = "Adam Bess", email = "bess@deepdrug.ai" },
    { name = "Chris Alvins", email = "calvin@deepdrug.ai" },
]
maintainers = [{ name = "Adam Bess", email = "bess@deepdrug.ai" }]
description = "A toolkit for fragmenting and analyzing molecular structures."
readme = "README.md"
keywords = ["chemistry", "molecule", "fragmentation", "bioinformatics"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Chemistry",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-cov"]

[project.urls]
Homepage = "https://deepdrug.ai"
Documentation = "https://readthedocs.org"
Repository = "https://github.com/deepdrugai/eMolFrag"
"Bug Tracker" = "https://github.com/deepdrugai/eMolFrag/issues"
Changelog = "https://github.com/deepdrugai/eMolFrag/blob/master/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/eMolFrag"]
exclude = ["**/__pycache__", "**/*.pyc"]


# Uncomment the below lines if you have scripts or GUI scripts to include
[project.scripts]
emolfrag = "eMolFrag.eMolFrag:main"

# [project.gui-scripts]
# eMolFrag-gui = "src.eMolFrag:main_gui"

# [project.entry-points."eMolFrag.magical"]
# magic = "src.representation:Molecule"
